# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-random-string

# Google OAuth
GOOGLE_CLIENT_ID=replace
GOOGLE_CLIENT_SECRET=replace

# Single-owner email (the advisor)
OWNER_EMAIL=your.email@example.com

# Database (SQLite for dev)
DATABASE_URL="file:./dev.db"

# Optional Zoom (Server-to-Server OAuth)
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# Optional SMTP for email confirmations
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="Advisor Assistant <no-reply@example.com>"
